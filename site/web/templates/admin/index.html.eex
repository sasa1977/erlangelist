<h1>Admin overview</h1>

<div class="container-fluid">
  <%= for {period_name, period_data} <- @all_visits do %>
    <div class="row">
      <h2><%= period_name %></h2>
      <%= for {visit_type, visit_data} <- period_data do %>
        <div class="col-md-3">
          <h3><%= visit_type %></h3>
          <table>
          <%= for {title, count} <- visit_data do %>
            <tr>
              <td><%= title %></td>
              <td><%= count %></td>
            </tr>
          <% end %>
        </table>
        </div>
      <% end %>
    </div>
  <%= end %>
</div>