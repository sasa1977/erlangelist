# Buffer

Example usage:

```
mix buffer.bench

# The default options are the same as:
#
#   mix buffer.bench \
#     --buffer-mod Buffer.Ets \
#     --buffer-size 200000 \
#     --operations 2000000 \
#     --pushes 15 \
#     --pulls 5
```
