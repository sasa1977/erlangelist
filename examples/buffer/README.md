# Buffer

Example usage:

```
mix buffer.prof

# The default options are the same as:
#
#   mix buffer.prof \
#     --buffer-mod Buffer.Ets \
#     --buffer-size 200000 \
#     --operations 2000000 \
#     --pushes 15 \
#     --pulls 5
```
